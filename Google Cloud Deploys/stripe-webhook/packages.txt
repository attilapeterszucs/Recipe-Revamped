{
    "name": "stripe-webhook-service",
    "version": "1.0.0",
    "description": "Standalone Google Cloud Function for Stripe webhook processing",
    "main": "index.js",
    "scripts": {
      "start": "functions-framework --target=stripeWebhook --port=${PORT:-8080}",
      "deploy": "gcloud functions deploy stripe-webhook --runtime nodejs20 --trigger-http --allow-unauthenticated --entry-point stripeWebhook --memory 512MB --timeout 60s",
      "test": "node test-webhook.js"
    },
    "dependencies": {
      "@google-cloud/functions-framework": "^3.3.0",
      "@google-cloud/firestore": "^7.1.0",
      "firebase-admin": "^12.0.0",
      "stripe": "^18.5.0"
    },
    "engines": {
      "node": "20"
    }
}